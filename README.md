# q_dev_resources

1. checkout from main branch to a practice one using git checkout -b cicd-pr
2. create .github folder and create yml files to do some workflow . create start.yml
3. use a sample code generated by q and on left side commit chnages and input msg as basic workflow

